name: Governance Lint

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  governance-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run governance lint
        run: python3 scripts/governance_lint.py
